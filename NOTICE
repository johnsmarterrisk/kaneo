Operon fork of Kaneo
====================

This repository is a fork of Kaneo, maintained by Smarter Risk as the
"Initiative" surface of Operon.

Upstream
--------
  Project     Kaneo — https://github.com/usekaneo/kaneo
  Fork point  tag v2.23.1, commit e13f512 ("chore(release): v2.23.1 [skip ci]")
  Forked on   2026-09-06
  Fork branch `operon` (this branch), cut from v2.23.1
  Fork remote https://github.com/johnsmarterrisk/kaneo

Licence
-------
Kaneo is distributed under the MIT Licence, Copyright (c) 2024 Andrej Acevski.
The upstream LICENSE file is retained VERBATIM and unmodified in this fork, and
it continues to govern all upstream code. Modifications made in this fork are
Copyright (c) 2026 Smarter Risk and are released under the same MIT Licence.

Modifications
-------------
Operon's fork discipline is that upstream is changed as little as possible: the
whole point of building on Kaneo is to keep Kaneo's feature set — boards,
backlog, labels, time tracking, webhooks, search and the MCP server — exactly as
upstream built it. Only eleven kinds of change are permitted, and every file this
fork touches is declared in Operon's `docs/fork-discipline.md`:

  1. Auth wiring — Operon is the OpenID Connect provider; Kaneo's own local
     sign-in is disabled by environment, and the post-login hook reports the new
     Kaneo user back to Operon.
  2. Branding and chrome — the product is called Initiative inside Operon, and
     the sidebar carries Operon's surface switcher.
  3. A `telegraph` integration type with an authorised external-link write
     route, so a Kaneo task can carry the Nostr event id of the chat message it
     came from.
  4. Workspace bootstrap — a single shared workspace, created by the first
     admin's login and joined by everyone after.
  5. Build hygiene — one `.dockerignore` line keeping a build-cache directory
     out of the image's context.
  6. Workspace permissions in Operon mode — an added member payload that lets
     members assign and delete tasks; upstream's own `member` payload is
     untouched, so an instance that is not Operon's behaves as it always did.
  7. Object storage — the presigned upload URL is rewritten onto an origin a
     browser outside the compose network can actually reach, and only when the
     deployment opts in.
  8. Egress policy — a private-destination allowlist for outbound webhook and
     Gitea targets, so a production deployment can permit exactly the internal
     receiver Operon runs instead of switching every private range back on with
     upstream's `KANEO_ALLOW_PRIVATE_WEBHOOK_DESTINATIONS` boolean.
  9. Backup quiescing — the sign-in path that mints and delivers Operon's
     service credential stands down while Operon is taking a consistent backup
     of its two stores, and reports what it has already admitted on two
     server-to-server routes so Operon can wait for it to drain. Nothing about
     Kaneo's own behaviour changes when Operon is not quiescing, and no delete
     path, board, task or object is gated.
 10. Terminal-status facts on the outbound task transition webhook — the
     generic-webhook payload for a status change or a move carries whether each
     status slug's COLUMN is final, because Kaneo keeps finality on the column
     (per project, configurable) while the payload carries slugs, and a
     consumer matching on the word would be wrong in both directions. A lookup
     that cannot be read omits the field rather than reporting `false`, so no
     other payload, event or Kaneo behaviour changes.
 11. Actor attribution on the outbound task-created webhook — the
     generic-webhook payload for a task creation names the person who CREATED
     the task. Kaneo's `task.created` event carries two people and names only
     the assignee `userId`, so the handler reporting it as the actor named the
     wrong person on an assigned task and nobody at all on an unassigned one.
     Upstream's meaning of `userId` is untouched, because Kaneo's own
     notification of the assignee depends on it; the creator travels beside it
     in a field the event bus previously dropped. An emitter that names no
     creator reports no actor rather than falling back to the assignee.

That count is RE-DERIVED from this branch's tree whenever it changes, never
incremented from a previous reading: `docs/fork-discipline.md` §3 carries the
touch list the eleven kinds map onto, path by path, and §7 records which task
landed each one. An inventory whose number nobody re-checked is what makes a
fork's provenance untrustworthy.

A fork commit that touches any path not declared in `docs/fork-discipline.md`
is a defect, not a feature: the touch list is what keeps this fork mergeable
with upstream.
